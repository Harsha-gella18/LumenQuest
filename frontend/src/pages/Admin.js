import React from 'react';
import { Card, CardContent, CardHeader } from '@mui/material';

const Admin = () => {
  return (
    <Card>
      <CardHeader title="Admin" />
      <CardContent>Admin dashboard placeholder</CardContent>
    </Card>
  );
};

export default Admin;
